services:
  unitdle:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      # host 8081 -> container nginx :80. Point your edge reverse proxy
      # (e.g. unitdle.doodlepros.com) at VM-IP:8081.
      - "8081:80"
